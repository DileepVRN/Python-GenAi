version: "3.8"

services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"

  account-service:
    build: ./account-service
    ports:
      - "8001:8001"
    depends_on:
      - mysql
